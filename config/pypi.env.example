# PyPI credentials for decp-core publishing
# Copy to config/pypi.env and fill in your token.
# config/pypi.env is gitignored — never commit secrets.

DECP_PYPI_USERNAME=__token__
DECP_PYPI_TOKEN=replace_with_your_pypi_api_token
DECP_PYPI_REPOSITORY_URL=https://upload.pypi.org/legacy/
